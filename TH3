if _G.VAR_HT3 then return end
_G.VAR_HT3 = true

task.wait(38)

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Client = ReplicatedStorage:WaitForChild("Library"):WaitForChild("Client")
local PlayerSave = require(Client.Save)

local function getSave()
    return PlayerSave.Get()
end

local enabled = false

while true do
    local sd = getSave()

    local rebirths = 0
    if sd.TapHeroes then
        rebirths = sd.TapHeroes.Rebirths or 0
    end

    if rebirths < 10 then
        if not enabled then
            enabled = true

            ReplicatedStorage.Network.Instancing_FireCustomFromClient:FireServer(
                "TapHeroes",
                "ZN_Auto",
                true
            )

            ReplicatedStorage.Network.Instancing_FireCustomFromClient:FireServer(
                "TapHeroes",
                "ZN_AutoRebirth",
                true
            )

            print("[TapHeroes] Rebirths:", rebirths, "-> Auto=true | AutoRebirth=true")
        end

    else
        ReplicatedStorage.Network.Instancing_FireCustomFromClient:FireServer(
            "TapHeroes",
            "ZN_Auto",
            true
        )

        ReplicatedStorage.Network.Instancing_FireCustomFromClient:FireServer(
            "TapHeroes",
            "ZN_AutoRebirth",
            false
        )

        print("[TapHeroes] Rebirths:", rebirths, "-> Auto=true | AutoRebirth=false")

        break
    end

    task.wait(1)
end
