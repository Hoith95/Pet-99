-- Auto Time Trial cho map 264 - phiên bản đơn giản, không claim reward, không phân biệt thắng/thua
task.spawn(function()

    task.wait(10)

    if game.PlaceId ~= 140403681187145 then
        return
    end

    local player = game.Players.LocalPlayer
    local Save = require(game.ReplicatedStorage.Library.Client.Save)

    local stopSignal = false
    local FARM_POSITION = CFrame.new(1556, 36, -648) -- Vị trí farm

    -- Hook TimeTrialResultUI.Show để phát hiện khi hoàn thành
    local success, timeTrialResultUI = pcall(function()
        return require(game.ReplicatedStorage.Library.Client.UI.TimeTrialResultUI)
    end)

    if success and typeof(timeTrialResultUI) == "table" and typeof(timeTrialResultUI.Show) == "function" then
        local originalShow = timeTrialResultUI.Show
        timeTrialResultUI.Show = function(arg1)
            warn("✅ Time Trial kết thúc")
            stopSignal = true
            return originalShow(arg1)
        end
    else
        warn("⚠️ Không thể hook vào TimeTrialResultUI.Show")
    end

    -- Dịch chuyển qua các điểm
    local function teleportSequence(player, sequence)
        for _, cframe in ipairs(sequence) do
            local character = player.Character
            if character and character:FindFirstChild("HumanoidRootPart") then
                character.HumanoidRootPart.CFrame = cframe
                task.wait(0.5)
            else
                break
            end
        end
    end

    -- Dịch chuyển đến part hoặc CFrame
    local function teleportTo(partOrCFrame)
        local character = player.Character
        if not character or not character:FindFirstChild("HumanoidRootPart") then return end

        if typeof(partOrCFrame) == "Instance" and partOrCFrame:IsA("BasePart") then
            character.HumanoidRootPart.CFrame = partOrCFrame.CFrame
        elseif typeof(partOrCFrame) == "CFrame" then
            character.HumanoidRootPart.CFrame = partOrCFrame
        end
    end

    -- Kiểm tra còn lượt Time Trial không, có chờ đến khi Save.Get() có dữ liệu
    local function canRunTrial()
        local saveData = nil
        for i = 1, 30 do
            saveData = Save.Get()
            if saveData and saveData.TimeTrialStats then
                break
            end
            task.wait(1)
        end
        if not saveData or not saveData.TimeTrialStats then
            warn("❌ Không thể lấy TimeTrialStats từ Save.")
            return false
        end

        local totalRuns = saveData.TimeTrialStats.TotalRuns or 0
        warn("👉 TotalRuns hiện tại: " .. totalRuns)
        return totalRuns < 10
    end

    -- Danh sách các điểm dịch chuyển trong map
    local teleportSequencePoints = {
        CFrame.new(-18079, 20, -484),
        CFrame.new(-18359, 20, -560),
        CFrame.new(-18306, 20, -695),
        CFrame.new(-18222, 20, -604),
        CFrame.new(-18221, 20, -458),
        CFrame.new(-18110, 20, -658),
    }

    -- Vòng lặp chính
    while true do
        if not canRunTrial() then
            warn("🚫 Đã đủ 10 lượt chạy hoặc không thể load dữ liệu. Dừng script.")
            break
        end

        stopSignal = false

        -- Teleport vào Time Trial
        teleportTo(workspace.__THINGS.Instances.TimeTrial.Teleports.Enter)
        task.wait(5)

        -- Dịch chuyển vòng quanh map
        while not stopSignal do
            teleportSequence(player, teleportSequencePoints)
        end

        -- Thoát khỏi Time Trial
        teleportTo(workspace.__THINGS.Instances.TimeTrial.Teleports.Leave)
        task.wait(5)

        -- Teleport về chỗ farm (không cần phân biệt thắng/thua)
        teleportTo(FARM_POSITION)
        warn("🏕️ Đã quay về vị trí farm.")

        task.wait(10)
    end

end)
